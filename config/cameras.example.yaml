# Camera list for multi-camera / RTSP (optional).
# To use: set CAMERA_SOURCES=yaml in .env and copy this file to config/cameras.yaml (or CONFIG_DIR/cameras.yaml).
# Requires PyYAML (pip install PyYAML). See docs/TECHNOLOGY_RESEARCH_AND_CONFIG.md.

cameras:
  - name: Front Door
    url: rtsp://user:password@192.168.1.100:554/Streaming/Channels/101
    # index: 0   # optional; use url OR index (OpenCV device index)
  - name: Garage
    url: rtsp://user:password@192.168.1.101:554/cam/realmonitor?channel=1&subtype=0
  - name: Driveway
    url: rtsp://admin:password@192.168.1.102:554/h264Preview_1_main

# Alternative: comma-separated in .env (no YAML)
# CAMERA_SOURCES=rtsp://user:pass@192.168.1.100:554/Streaming/Channels/101,rtsp://user:pass@192.168.1.101:554/cam/realmonitor?channel=1&subtype=0
